# 简易日程提醒 App

一个纯前端的轻量日程提醒应用，支持：

- 添加提醒事项（标题 + 时间）
- 本地存储（刷新后不丢失）
- 到点通知（浏览器通知；若权限未开启则弹窗）
- 标记完成 / 恢复、删除、清空

## 运行方式

直接打开 `index.html`，或使用静态服务器：

```bash
python3 -m http.server 4173
```

然后访问 `http://localhost:4173`。
