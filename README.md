# Python 桌面待办事项工具

一个基于 **Tkinter** 的桌面待办工具，界面为深色卡片风格，支持任务分类、筛选和到期提醒。

## 功能

- 新增 / 编辑 / 删除任务
- 自定义任务分类
- 按分类和完成状态筛选
- 支持截止时间（`YYYY-MM-DD HH:MM`）
- 到期前提醒弹窗（每 30 秒检查一次）
- 本地 JSON 持久化存储

## 运行方式

```bash
python3 app.py
```

## 数据文件

程序会在当前目录自动生成：

- `todo_data.json`：保存任务与分类数据

